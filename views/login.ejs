<!DOCTYPE html>
<html lang="en">
<head>
    <style media="screen">
        *,
        *:before,
        *:after {
            padding: 0;
            margin: 0;
            box-sizing: border-box;
            color: #000000; /* Set text color to black */
        }
        body {
            /* margin: 0;
            padding: 0; */
            box-sizing: border-box;
            display: flex;
            flex-direction: column; /* Ensure vertical stacking of elements */
            align-items: center;
            background:  linear-gradient(45deg, rgb(13, 14, 13), dodgerblue);
            font-family: "Sansita Swashed", cursive;
        }
        .header {
            text-align: center;
            margin-bottom: 10px;
            color: #f50b0b; /* Set text color to red */
            z-index: 1; /* Ensure the header appears above other elements */
            background-color: rgba(255, 255, 255, 0.9); /* Set background color for better contrast */
            width: 100%;
            padding: 10px 0;
        }
        form {
            height: 520px;
            width: 400px;
            background-color: #1a1a1a;
            position: relative; /* Set position to relative for stacking elements */
            border-radius: 10px;
            backdrop-filter: blur(10px);
            border: 2px solid rgba(255, 255, 255, 0.1);
            box-shadow: 0 0 40px rgba(8, 7, 16, 0.6);
            padding: 50px 35px;
            color: #000000; /* Set text color to black */
            z-index: 0; /* Ensure the form appears below the header */
        }
        form * {
            font-family: 'Poppins', sans-serif;
            color: #000000; /* Set text color to black */
            letter-spacing: 0.5px;
            outline: none;
            border: none;
        }
        label {
            display: block;
            margin-top: 30px;
            font-size: 16px;
            font-weight: 500;
            color: #ffffff; /* Set label text color to white */
        }
        input {
            display: block;
            height: 50px;
            width: 100%;
            background-color: rgba(255, 255, 255, 0.07);
            border-radius: 3px;
            padding: 0 10px;
            margin-top: 8px;
            font-size: 14px;
            font-weight: 300;
        }
        ::placeholder {
            color: #e5e5e5;
        }
        button {
            margin-top: 30px;
            width: 100%;
            background-color: #ffffff;
            color: #080710;
            padding: 15px 0;
            font-size: 18px;
            font-weight: 600;
            border-radius: 5px;
            cursor: pointer;
        }
        .social {
            margin-top: 20px;
            display: flex;
            justify-content: space-between;
        }
        .social div {
            width: 48%;
            border-radius: 3px;
            padding: 5px 10px 10px 5px;
            background-color: rgba(255, 255, 255, 0.27);
            color: #eaf0fb;
            text-align: center;
        }
        .social div:hover {
            background-color: rgba(255, 255, 255, 0.47);
        }
        .social i {
            margin-right: 4px;
        }
      </style>
</head>
<body>
    <div class="header" style="color: #f50b0b; background-color: transparent;">
        <h2>Only admin can login</h2>
    </div>

    <form action="./login" method="POST">
        <label for="name">Name</label>
        <input type="text" placeholder="Name" id="name" name="name">

        <label for="password">Password</label>
        <input type="password" placeholder="Password" id="password" name="password">

        <button type="submit">Log In</button>
        <p>Don't have an account? <a href="/sign_up" class="icon-a">Sign up</a></p>
    </form>
</body>
</html>
